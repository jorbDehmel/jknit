```{settings}
python = /usr/bin/python
octave = /usr/bin/octave-cli
```

\title{JKnit}
\author{Jordan Dehmel}

\maketitle{}

Hi there! This is a test
of the jknit
system!

----------

$$
\begin{aligned}
    1 + 1 &= 2 \\
    2 + 1 &= 3 \\
\end{aligned}
$$

This should not be math. Some python code and its output:

```{python}
print('hello, world!')
```

Some Octave code and its output:

```{octave}
0 + 5 + 12 + -100
```

The above code is code, probably. Inline math: $1 + 5 + 6 = 12$.
